spring:
  datasource:
    url: jdbc:mysql://localhost:3306/store # قاعدة بيانات محلية صريحة
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # جيد للتطوير
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  websiteUrl: http://localhost:4242 # مثال على Website URL للتطوير